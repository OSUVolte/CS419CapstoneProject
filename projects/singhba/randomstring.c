/*
To get error message; input string needs to return word 'reset'
For this I implemented inputChar() to return a char randomly
and inside inputString() I used inputChar() to create string
and that string is returned to testme(). Once testme() gets 
'reset' from inputString() , it prints
error message and exit the program. 
*/
