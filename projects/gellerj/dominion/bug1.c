/*
 * VILLAGE: The test which checks the number of actions in the current game state fails due to a bug that was
 * introduced during the first assignment. Village is supposed to increment the number of actions by 2, but due to
 * the bug, does not increment, but rather replaces the state.
 *
 */