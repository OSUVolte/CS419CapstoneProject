game.BuildingManager = me.Container.extend({
    init: function (x,y) {
        //initialize the first building

        //this._super(me.Container, "init", [0, 32, this.COLS * 64 - 32, this.ROWS * 64 - 32]);
       // this.z = 5; // keep it above

        // //this.body.collisionType = me.collision.types.WORLD_SHAPE; // dont hit other things?
        // this.renderable = new (me.Renderable.extend({
        //     init: function () {
        //         this._super(me.Renderable, "init", [0, 0, game.building.width, game.building.height]);
        //     }
        // }
    }


    //Todo Add functions that are the logic for all buildings

    //what happens on building destroy

    //what happens when user kill building

    //what happens when....

});